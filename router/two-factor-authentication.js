const Router = require('koa-router');
const router = new Router();
const connection = require('../app/db');

router.get('/two-factor-authentication', async (ctx) => {
    
});

module.exports = router;
